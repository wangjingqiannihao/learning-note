# Linux 常用命令

查看当前目录下各文件/目录的磁盘占用，并按 K/M/G 等单位从大到小排序。

```bash
du -sh * | sort -rh
```

## 区分 cgroup v1 和 cgroup v2

直接查看 `/sys/fs/cgroup` 的文件系统类型。

```bash
stat -fc %T /sys/fs/cgroup
```

输出为 `cgroup2fs` 表示当前使用 cgroup v2；输出为 `tmpfs` 表示当前使用 cgroup v1。
